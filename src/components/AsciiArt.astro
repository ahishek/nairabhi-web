---
---

<div class="ascii-art-container">
  <div class="human-only">
    <div class="ascii-scroll-wrap">
      <pre class="ascii-art">█████╗ ██████╗ ██╗  ██╗██╗███████╗██╗  ██╗███████╗██╗  ██╗  ███╗   ██╗ █████╗ ██╗██████╗
██╔══██╗██╔══██╗██║  ██║██║██╔════╝██║  ██║██╔════╝██║ ██╔╝  ████╗  ██║██╔══██╗██║██╔══██╗
███████║██████╔╝███████║██║███████╗███████║█████╗  █████╔╝   ██╔██╗ ██║███████║██║██████╔╝
██╔══██║██╔══██╗██╔══██║██║╚════██║██╔══██║██╔══╝  ██╔═██╗   ██║╚██╗██║██╔══██║██║██╔══██╗
██║  ██║██████╔╝██║  ██║██║███████║██║  ██║███████╗██║  ██╗  ██║ ╚████║██║  ██║██║██║  ██║
╚═╝  ╚═╝╚═════╝ ╚═╝  ╚═╝╚═╝╚══════╝╚═╝  ╚═╝╚══════╝╚═╝  ╚═╝  ╚═╝  ╚═══╝╚═╝  ╚═╝╚═╝╚═╝  ╚═╝</pre>
    </div>

    <div class="tagline">
      <span class="tagline-text">$ Product Manager, Tinkerer & wannabe developer</span
      ><span class="cursor">_</span>
    </div>
  </div>

  <div class="agent-only">
    <div class="agent-title"># Abhishek Nair</div>
    <div class="agent-subtitle">Product Manager, Tinkerer & wannabe developer</div>
  </div>
</div>


<style scoped>
  .ascii-art-container {
    width: 100%;
    padding: 2rem 1rem;
  }

  .human-only {
    display: block;
  }

  .ascii-scroll-wrap {
    width: 100%;
    text-align: center;
    overflow-x: hidden;
  }

  .agent-only {
    display: none;
  }

  :global(.machine-mode) .human-only {
    display: none;
  }

  :global(.machine-mode) .agent-only {
    display: block;
  }

  .ascii-art {
    color: var(--text-green);
    font-family: var(--font-mono);
    font-size: clamp(0.46rem, 0.86vw, 0.72rem);
    line-height: 1.2;
    margin: 0;
    padding: 1rem 0;
    text-align: left;
    white-space: pre;
    display: inline-block;
    max-width: none;
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.3), 0 0 20px rgba(0, 255, 65, 0.1);
    letter-spacing: 0;
  }

  .tagline {
    text-align: center;
    margin-top: 1.5rem;
    font-family: var(--font-mono);
    font-size: 1rem;
    color: var(--text-green);
  }

  .tagline-text {
    display: inline;
  }

  .cursor {
    display: inline-block;
    margin-left: 0.25rem;
    animation: blink 1.2s infinite;
  }

  @keyframes blink {
    0%,
    49% {
      opacity: 1;
    }
    50%,
    100% {
      opacity: 0;
    }
  }

  .agent-title {
    text-align: center;
    font-size: 2rem;
    font-weight: bold;
    color: var(--text-amber);
    font-family: var(--font-mono);
    text-shadow: 0 0 10px rgba(255, 176, 0, 0.2);
  }

  .agent-subtitle {
    text-align: center;
    font-size: 1.1rem;
    color: var(--text-green);
    font-family: var(--font-mono);
    margin-top: 0.5rem;
  }

  /* Desktop: no scroll; Mobile: horizontal scroll allowed. */
  @media (max-width: 768px) {
    .ascii-art-container {
      padding: 1.5rem 0.5rem;
    }

    .ascii-scroll-wrap {
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
    }

    .ascii-art {
      font-size: clamp(0.44rem, 1.3vw, 0.66rem);
      padding: 0.5rem 0;
    }

    .tagline {
      font-size: 0.9rem;
      margin-top: 1rem;
    }

    .agent-title {
      font-size: 1.5rem;
    }

    .agent-subtitle {
      font-size: 0.95rem;
    }
  }

  @media (max-width: 480px) {
    .ascii-art {
      font-size: clamp(0.38rem, 1.8vw, 0.56rem);
      padding: 0.25rem 0;
      line-height: 1;
    }

    .tagline {
      font-size: 0.75rem;
      margin-top: 0.75rem;
    }

    .agent-title {
      font-size: 1.2rem;
    }

    .agent-subtitle {
      font-size: 0.85rem;
    }
  }
</style>
